# M4 装扮 + 声音 + 纪念日(W7-W8)

> 骨架,M4 入口前由 module-implementer 拆完整 stories。

- **入口**:M3 出口达成 + 配饰美术(8 件 + 4 套节气)交付 + 音效包(12-20 条 OGG)录制完成 + **新 ADR-016 VRM 配饰美术规范 Accepted**(humanoid bone 命名 / .vrma 节点 / VRMC_node_constraint / z 轴排序)
- **出口必达**:装扮切换 < 500ms / 声音工作日 09:00-18:00 严格 0 触发 / 用户纪念日时区跨日正确
- **关联**:[路线图 §5.5](../docs/AIPET-obsidian/2026-05-01-development-roadmap-v1.0.md)

---

## 入口前置(M3 末完成)

- [ ] **ADR-016 VRM 配饰美术规范** 起草并 Accepted(走 adr-author 角色)
  - humanoid bone 命名约定(head / neck / leftEye / rightEye / chest 等)
  - 配饰资源格式:`.glb` 节点 / `.vrma` 动画
  - VRMC_node_constraint 配置(rotation / aim / position 等)
  - z 轴排序约定(0-3 件叠加时谁在前谁在后)
  - 内置 8 件配饰 + 4 套节气皮肤的产品定稿(品类 / 风格)

---

## 模块占用

| Module | Subject | 关键 ADR / PRD |
|---|---|---|
| **O.1 配饰系统** | 8 件配饰 list_inventory + equip + VRM bone attach + VRMC_node_constraint | ADR-003(Superseded → 用 ADR-016 替代)/ ADR-011 |
| **O.2 节气皮肤** | 4 套节气整体替换(每天 00:01 检查,默认每节气 1 次) | PRD §6.O.2 |
| **O.3 付费 schema 预埋** | `tier='paid'` 强制过滤,MVP 期不返前端 | ADR-011 |
| **装扮工坊前端** | 0-3 件叠加 UI + 实时预览 | PRD §6.O |
| **P 声音表情** | VoiceEffectPlayer(HTML5 Audio)+ 音效包(12-20 OGG)+ 静音时段(工作日 09-18) + 音量控制(0-100,默认 50) | ADR-010 / PRD §6.P |
| **S.4 用户纪念日** | `user_anniversaries` 表 + MilestoneService 扩展 + 纪念日 UI(设置 → 我的纪念日)+ 年度键 `anniversary_<key>_<YYYY>` | PRD §6.S.4 / 路线图 风险 r8 |

---

## 出口判断(M4 → M5)

### 必达项
- [ ] 装扮切换 < 500ms(0 件 → 1 件 / 1 件 → 3 件 / 3 件 → 0 件)
- [ ] 声音工作日 09:00-18:00 严格 0 触发(自动化测试用伪时钟跑 1 天)
- [ ] 用户纪念日时区跨日正确(单测覆盖 UTC+8 跨午夜场景)
- [ ] 节气推送默认每节气仅 1 次(用户拒绝当年不再推)

### 可妥协项
- 装扮工坊 UX 细节(M5 调)

---

## 关键风险

- r9 VRM 渲染内存超 250MB(LOD 切换 / 低多边形 / 低分辨率贴图模式兜底)
- r10 节气推送被认为打扰(灰度数据驱动)
- r11 DPAPI 跨用户切换异常(作为 feature 暴露,账户绑定)
