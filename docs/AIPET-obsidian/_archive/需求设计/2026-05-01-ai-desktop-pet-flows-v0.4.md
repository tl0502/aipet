# AI桌宠 流程与状态机（v0.4）

- 文档版本：v0.4
- 创建日期：2026-05-01
- 替代：v0.3
- 关联：PRD v0.4、角色与人格设计 v0.1、系统架构 v0.1
- 相对 v0.3 的核心变更：
  1. 新增 Onboarding 详细流程（6 步）。
  2. 新增并发优先级（FOCUS + REMIND）。
  3. 新增人格切换流程。
  4. 新增数据迁移流程。
  5. 新增异常恢复流程。
  6. 状态机增加 ERROR、UPDATING、FIRST_RUN、ONBOARDING。

---

## 1. 首次启动 Onboarding 流程（v0.4 详化）

### 1.1 触发条件
本地无配置文件 / 配置版本不识别 / 用户主动重置。

### 1.2 6 步引导

```
启动
 ↓
[检测本地配置]
 ↓
 ├── 存在 → 进入主态
 └── 不存在 ↓
        Step 1：欢迎与隐私同意
        ├── 显示数据策略（本地优先、可清除、不上传）
        ├── 用户同意 → 继续 / 拒绝 → 退出应用
        ↓
        Step 2：选择人格
        ├── 展示 3 个内置人格卡片（默默 / 阿吉 / 教官）
        ├── 用户选择 → 设为 active persona
        └── 跳过 → 默认"默默"
        ↓
        Step 3：确认快捷键
        ├── 默认 Ctrl+Alt+Space
        ├── 探测当前快捷键是否被占用 → 占用则提示改键
        └── 用户确认 / 改键
        ↓
        Step 4：选择默认提醒模板
        ├── 多选：喝水 / 久坐 / 学习专注 / 不需要
        ├── 选中后批量创建提醒（用户可在 Step 5 之后随时关）
        ↓
        Step 5：第一次番茄钟（可选）
        ├── 一句话引导："要不试 5 分钟微专注?"
        ├── 用户接受 → 启动 5min 番茄钟
        └── 跳过 → 继续
        ↓
        Step 6：第一次唤起对话（可选）
        ├── 显示快捷键浮窗 3 秒
        ├── 用户按下快捷键 → 弹对话框 → 引导发第一句
        └── 5 秒未操作 → 跳过
        ↓
[创建默认配置 + 标记 onboarded=true]
 ↓
[进入主态，桌宠出现，状态 IDLE]
```

### 1.3 失败分支

| 失败点 | 处理 |
|---|---|
| 本地库初始化失败 | 重试 1 次 → 提示 + 显示 logs 路径 → 退出 |
| 快捷键探测失败（系统拒绝注册） | Step 3 提示用户尝试其他组合，至少必须注册成功一组 |
| Live2D 资源加载失败 | 降级为静态 PNG 立绘 + 上报错误 |

### 1.4 中途退出
任意 Step 关闭窗口 → 配置不写入 → 下次启动重头开始。
保护用户"半完成"状态不会留下脏数据。

---

## 2. 快捷唤起对话流程（v0.4 增强）

```
[全局快捷键监听]
 ↓
[命中]
 ↓
 ├── 主进程检查桌宠状态
 │   ├── ONBOARDING / FIRST_RUN → 忽略，提示稍后再试
 │   ├── UPDATING → 忽略，提示更新中
 │   └── 其他 → 继续
 ↓
[弹出 chat 面板，定位在桌宠附近，聚焦输入框]
 ↓
[用户输入]
 ├── ESC / 失焦 / 点击外部 → 关闭面板，不写入消息
 ├── Enter → 发送
 └── Shift+Enter → 换行
 ↓
[发送]
 ↓
[ChatService 拼装 prompt]
 ├── 安全前缀 + 当前人格 system prompt + 记忆摘要 + 历史 + 本轮输入
 ↓
[判断网络状态]
 ├── 在线 → LLMProvider.chat_stream
 │   ├── 流式返回 chat.token → 前端逐字渲染
 │   ├── 完成 → chat.done
 │   ├── 超时（> 8s 无 token）→ 切离线规则
 │   └── 用户取消（再按快捷键 / 点取消）→ chat.cancel
 └── 离线 → 命令词解析 → 命中？
     ├── 是 → 直接执行（如"开始番茄钟"）
     └── 否 → 情感分类 → 命中人格离线模板池 → 返回模板
 ↓
[写入消息记录]
 ↓
[更新对话视图]
```

### 2.1 失败分支

| 分支 | 处理 |
|---|---|
| 快捷键注册失败 | 启动期提示用户改键 |
| LLM 返回错误（401/429/500） | 显示错误代码 + 引导设置页 / 稍后重试 |
| LLM 超时无 token > 8s | 自动降级到离线规则模式 |
| 流式中网络中断 | 已收 token 保留，错误提示，整条消息状态标 partial |
| API Key 解密失败 | 提示用户重新设置 API Key |

---

## 3. 提醒闭环（v0.4 新增并发优先级）

### 3.1 创建到触发

```
[用户创建提醒]
 ├── 一次性 / 每日 / 每周 / cron
 └── priority: soft（默认）/ hard
 ↓
[Scheduler 计算 next_fire_at]
 ↓
[到点]
 ↓
[查询当前桌宠状态]
 ↓
 ├── 当前是 FOCUS 且 priority=soft
 │   └── 进入"软提醒缓冲"（不打断专注）
 │       └── FOCUS 结束（→ REST）时合并展示所有缓冲软提醒
 │
 ├── 当前是 FOCUS 且 priority=hard
 │   └── 立即进入 REMIND 状态（强行打断专注，番茄钟暂停）
 │
 └── 其他状态（IDLE / REST / REMIND）
     └── 立即进入 REMIND 状态
 ↓
[展示提醒：通知 + 桌宠动作 + 内联消息]
 ↓
[用户响应]
 ├── 完成 → snooze_count = 0 → 写历史 → REMIND → IDLE
 ├── 稍后 10min → snooze_count + 1
 │   ├── snooze_count > 3 → 标记 overdue + 写复盘列表 → REMIND → IDLE
 │   └── 否则 → next_fire_at = now + 10min → REMIND → IDLE
 └── 忽略 → 写历史 → REMIND → IDLE
```

### 3.2 软提醒缓冲合并展示

`FOCUS → REST` 时，如果有 ≥ 2 条缓冲软提醒，桌宠用一句话合并："专注完成，提醒你：喝水 + 起身活动。"

### 3.3 用户暂停番茄期间的提醒
番茄钟暂停时按 IDLE 处理（即所有提醒都立即触发）。

### 3.4 失败分支

| 分支 | 处理 |
|---|---|
| 系统通知权限被关 | 仍以桌宠动作 + 应用内消息形式提醒 |
| 进程被强杀后重启 | 启动期 Scheduler 扫描 missed reminders，对最近 30min 内的过期提醒"补提" |
| 系统休眠唤醒 | 唤醒时校准 next_fire_at；过期 ≤ 30min 的合并补提，超过的标 overdue |

---

## 4. 番茄钟流程（v0.4 增强）

```
[用户启动番茄钟]
 ├── 默认 25/5（可在工坊配置 5–90 / 1–30）
 ↓
[IDLE → FOCUS] + Scheduler 启动倒计时
 ↓
[Tick → 'pomodoro.tick' 事件 → 前端更新计时]
 ↓
 ├── 用户暂停
 │   ├── FOCUS → PAUSED（新增中间态，不进入正式状态机）
 │   └── 用户恢复 → 回到 FOCUS，剩余时间继续
 ├── 用户提前结束
 │   ├── 已完成 ≥ 30% 时长 → 计为完成（半个番茄）
 │   └── 否则 → 计为放弃
 │   → 写 pomodoro_sessions.status='cancelled' → IDLE
 ├── 系统休眠
 │   └── 唤醒后剩余时间按"休眠之前的剩余 - max(0, 休眠耗时)"计算（避免休眠太久后秒结束）
 └── 倒计时结束
     ├── FOCUS → REST + 触发桌宠动作 + 通知
     └── REST 结束 → REST → IDLE
```

### 4.1 状态字段

`pomodoro_sessions.status`: `running | paused | completed | cancelled`

---

## 5. 网络状态切换（v0.4 微调）

```
[NetworkProbe 持续探测]
 ↓
[在线 → 离线]（连续 2 次失败或系统报告离线）
 ├── 触发 'network.changed' { online: false, mode: 'offline_rule' }
 ├── 前端显示横幅："已切换到离线规则模式"
 ├── 当前正进行的对话 → 标 partial → 提示用户
 └── ChatService 内部模式切换
 ↓
[离线 → 在线]（连续 2 次探测成功）
 ├── 触发 'network.changed' { online: true, mode: 'online_chat' }
 ├── 前端隐藏横幅
 ├── TelemetryService 触发 flush 补发
 │   ├── 成功 → telemetry_queue 标记 flushed
 │   └── 失败 → retry_count + 1，retry_count > 3 时保留并标失败原因
 └── 不主动重发用户对话
```

---

## 6. 人格切换流程（v0.4 新增）

```
[用户从托盘 / 对话面板 / 工坊触发 persona.activate(id)]
 ↓
[PersonaService 读取目标 .soul.md]
 ├── 解析失败 → 错误提示，不切换
 └── 成功 → 继续
 ↓
[校验目标人格]
 ├── 安全前缀仍然兼容 → 通过
 └── 不通过 → 错误提示
 ↓
[更新 personas 表 is_active 字段（事务）]
 ↓
[前端：替换桌宠形象]
 ├── 卸载当前 Live2D 模型
 ├── 加载目标人格 avatar.pack
 └── 加载失败 → 静态降级 + 上报
 ↓
[ChatService：刷新 prompt 缓存]
 ├── 当前对话历史保留（用户体验：不清空记忆）
 └── 下条消息使用新人格的 system prompt
 ↓
[触发 'persona.activated' 事件 → 前端更新 UI]
 ↓
[桌宠播一句"上线问候"（来自新人格的离线模板池 ## 问候）]
```

### 6.1 试聊沙盒分支

```
[用户在工坊编辑中，点"试聊沙盒"]
 ↓
[创建 sandbox conversation（is_sandbox=1）]
 ↓
[使用未保存的 draftMd 作为人格，正常拼装 prompt]
 ↓
[对话 3-5 轮，所有消息写入 sandbox 会话]
 ↓
[用户保存人格 → 沙盒会话归档]
[用户取消 → 沙盒会话连同消息删除]
```

---

## 7. 数据迁移流程（v0.4 新增）

### 7.1 启动时检查

```
[启动]
 ↓
[查询 schema_version 表]
 ↓
[读取 migrations/ 目录下的迁移文件清单]
 ↓
[对比 → 计算待应用的迁移列表]
 ↓
 ├── 空 → 跳过
 └── 非空 ↓
        [备份当前 db → backup/db-<v>-<timestamp>.bak]
        ↓
        [按序执行每个迁移（事务）]
        ├── 任一失败 → ROLLBACK + 删除当前 db + 恢复备份 + 显示错误退出
        └── 全部成功 → 写入 schema_version
        ↓
        [清理超过 5 个的旧备份]
```

### 7.2 用户数据导入

```
[用户拖入 export.json]
 ↓
[校验 export.schema_version]
 ├── 高于当前 → 拒绝（不向后兼容）
 ├── 低于当前 → 启动期再走一遍升级链
 └── 等于 → 直接导入
 ↓
[展示预览：将导入 N 个人格、M 条记忆、X 条提醒...]
 ↓
[用户确认 → 事务写入]
```

---

## 8. 异常恢复流程（v0.4 新增）

### 8.1 进程崩溃 / 强杀

```
[进程退出（非主动）]
 ↓
[下次启动]
 ↓
[启动期检查最近一次 startup_id 是否有正常 shutdown 标记]
 ↓
 ├── 是 → 正常启动
 └── 否 → 进入"恢复模式"
        ├── 扫描 reminders.next_fire_at < now 的 → 补提（30min 内）/ 标 overdue（更早）
        ├── 扫描 pomodoro_sessions.status='running' 的 → 标 cancelled
        ├── 扫描 messages.status='streaming' 的 → 标 partial
        └── 写 startup_id + 进入主态
```

### 8.2 数据库损坏

```
[启动期打开 db 失败]
 ↓
[检查 backup/ 下最近一个备份]
 ├── 存在且不超过 7 天 → 提示用户"恢复到 X 时间的数据" → 用户同意 → 恢复
 └── 否则 → 提示用户"数据库损坏，请联系支持 / 重置应用"
```

### 8.3 LLM 配置失效

```
[ChatService 调用失败：401 / 403 / 余额不足]
 ↓
[展示错误：API Key 失效，请到设置更新]
 ↓
[临时切换到离线规则模式，不阻断用户继续使用其他功能]
```

---

## 9. 自动更新流程（v0.4 新增）

```
[启动 + 每天 1 次] UpdaterService.check()
 ↓
[请求 manifest]
 ↓
 ├── 当前已是最新 → 退出
 └── 有新版本 ↓
        [判断 mandatory]
        ├── true → 立即弹强制对话框，倒计时 5s 自动开始
        └── false → 弹气泡（"晚点说" / "现在更新"）
        ↓
        [下载差分包]
        ├── 失败 → 静默重试 3 次 → 24h 后再说
        └── 成功 ↓
               [校验签名]
               ↓
               [触发 'updater.available' 事件]
               ↓
               [用户点"现在更新"]
               ↓
               [关闭对话面板 / 工坊 / 设置 → 保留桌宠]
               ↓
               [PET 进入 UPDATING 状态 → 应用安装 → 重启]
```

---

## 10. 核心状态机（v0.4 重写）

### 10.1 桌宠主状态

```
                              ┌─────────────┐
                              │   BOOTING   │
                              └─────┬───────┘
                                    │ 配置就绪
                       ┌────────────┴──────────────┐
                       │                            │
                       ▼                            ▼
                ┌────────────┐              ┌──────────────┐
                │ ONBOARDING │  Step 6 完成 │     IDLE     │◀──┐
                │            │ ───────────► │              │   │
                └────────────┘              └─┬─────┬──────┘   │
                                              │     │           │
                                              │     │ 番茄钟开始 │
                          完成提醒 / 忽略 / 稍后│     ▼           │
                                              │   ┌─────┐       │
                                              │   │FOCUS│       │
                                              │   └──┬──┘       │
                                              │      │ 倒计时完成│
                                              │      ▼          │
                                              │   ┌─────┐       │
                                              │   │REST │───────┘
                                              │   └──┬──┘ 休息结束
                                              │      │
                                              │      │ 硬提醒
                                              ▼      ▼
                                          ┌────────────┐
                                          │   REMIND   │
                                          └────────────┘

       ┌──────────┐           ┌──────────┐
       │ UPDATING │           │  ERROR   │
       └──────────┘           └──────────┘
        （任意态可入：用户触发更新 / 致命错误）
```

### 10.2 状态清单

| 状态 | 含义 | 进入条件 | 退出条件 |
|---|---|---|---|
| `BOOTING` | 启动中 | 进程启动 | 配置加载完成 |
| `ONBOARDING` | 首启引导 | 检测无配置 | Step 6 完成或跳过 |
| `IDLE` | 空闲 | 默认态 | 番茄开始 / 提醒触发 / 进入更新 |
| `FOCUS` | 专注 | 番茄钟启动 | 倒计时完成 / 用户结束 / 硬提醒打断 |
| `REST` | 休息 | FOCUS 结束 | 休息倒计时 / 用户跳过 |
| `REMIND` | 提醒中 | 提醒触发 | 用户响应 |
| `UPDATING` | 更新中 | 用户触发更新 | 重启或用户取消 |
| `ERROR` | 致命错误 | 数据库不可恢复 / 必要权限被拒 | 用户处理 / 重启 |

### 10.3 状态迁移规则

| From → To | 条件 |
|---|---|
| `BOOTING → ONBOARDING` | 检测到首次启动 |
| `BOOTING → IDLE` | 配置存在 |
| `ONBOARDING → IDLE` | 完成或跳过 Step 6 |
| `IDLE → FOCUS` | 用户启动番茄钟 |
| `FOCUS → REST` | 倒计时完成 |
| `FOCUS → IDLE` | 用户提前结束 / 硬提醒打断后处理 |
| `FOCUS → REMIND` | 硬提醒触发 |
| `REST → IDLE` | 休息倒计时完成 / 用户跳过 |
| `IDLE / REST → REMIND` | 软或硬提醒触发 |
| `REMIND → 上一态` | 用户处理提醒（带"返回"语义） |
| `* → UPDATING` | 用户接受更新 |
| `UPDATING → BOOTING` | 应用重启 |
| `* → ERROR` | 致命错误 |

### 10.4 对话模式状态

| 状态 | 含义 |
|---|---|
| `ONLINE_CHAT` | 在线，使用 LLM Provider |
| `OFFLINE_RULE` | 离线，使用规则 + 人格化模板 |

迁移由 `NetworkProbe` 驱动（见 §5）。

---

## 11. UAT 关键场景对应（与埋点UAT文档联动）

| 场景 | 对应流程 |
|---|---|
| 首启完整 onboarding | §1 |
| 软/硬提醒优先级 | §3 |
| 试聊沙盒不污染正式记忆 | §6.1 |
| 进程被强杀后提醒不丢 | §8.1 |
| 网络切换不导致崩溃 | §5 |
| 数据迁移失败可回滚 | §7.1 |
| 人格切换不丢记忆 | §6 |

---

## 12. 实施提示

- 状态机推荐使用 Rust 侧 enum + match 严格穷尽，新增状态时编译期强制处理。
- 跨服务的状态查询统一通过 `AppState` 单例（带读写锁），避免散落。
- 所有状态转移都应触发埋点事件 `pet.state_changed`，便于回归。
